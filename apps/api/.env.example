# Port the API listens on.
PORT=4000

# In production, point these at managed infra (state is in-memory in this build).
# DATABASE_URL=postgres://user:pass@localhost:5432/soltrend
# REDIS_URL=redis://localhost:6379
