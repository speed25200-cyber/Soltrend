[package]
name = "house_vault"
version = "0.1.0"
description = "Soltrend house vault, bet settlement and creator revenue split"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "house_vault"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = { version = "0.30.1", features = ["init-if-needed"] }
